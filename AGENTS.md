# AGENTS.md — 《岳桩山》开发约定

> 本文件是给协作者（人或 AI agent）的项目级指南。先读本文件，再读 `docs/`。

## 项目性质

《岳桩山》是一款**中文拟真网站型恐怖 ARG 网页解谜游戏**。当前为**第一阶段纵向切片**（约 15 分钟开场）。

## 技术栈（不可擅自更换）

- Vite + TypeScript，静态多页面。
- **禁止**引入 React、后端、数据库、复杂游戏引擎。
- 每个虚构网站 = 一个独立 HTML 页面（保持"拟真独立网站"观感，每个站点是真实 URL）。

## 权威文档（修改前必读，冲突时以此为准）

- `docs/world_bible.md` —— 世界观圣经。所有文案、谜题不得与之矛盾。
- `docs/timeline.md` —— 时间线。所有日期/时间必须一致。
- `docs/clue_graph.md` —— 线索依赖图。每条线索的来源/结论/解锁。
- `docs/game_design.md` —— 机制、架构、状态模型、输入标准化、无障碍。

**修改顺序**：先改对应 docs → 再改 `src/data/` → 再改 `src/pages/` → 跑 `npm test && npm run build`。

## 架构铁律

1. **剧情文案集中**在 `src/data/content.ts`，不在 HTML 硬编码大段剧情。
2. **答案集中**在 `src/data/clues.ts`（标准化后存储），方便测试维护。
3. **口令判断/搜索匹配/进度判断**全部走 `src/shared/*`，不散落各页面。
4. **答案无法真正保密**——客户端源码可查；只有明确设计为"源码谜题"时才允许把答案放源码。
5. 新增页面：在 `vite.config.ts` 的 `entries` 注册，在 `src/data/pages.ts` 加元数据。

## 输入标准化（核心可维护性）

所有搜索/口令输入必须经 `src/shared/normalize.ts`：
- 去首尾空白 → 全角转半角 → 去分隔符 → 转小写。
- 有限同义词表（`SYNONYMS`）避免"换个说法就找不到"。
- 答案与玩家输入走**同一条**标准化路径。

## 无障碍（必须满足）

- 音频默认静音；提供音量/静音/减少动态/字幕开关。
- 闪烁 ≤ 3Hz；`reduce-motion` 下完全静止。
- 键盘可达；正文对比度 ≥ 4.5:1。
- 不伪造真实系统警告，不读取真实个人信息。

## 内容红线

- 所有人物、机构、地址、电话、账号、网站**均为虚构**。
- **不**复制现实产品（微信、小红书等）的名称/Logo/界面；用虚构平台（见 `FICTIONAL_BRANDS`）。
- "合奘教"必须完全虚构，不影射现实宗教。

## 开发命令

```bash
npm install          # 安装依赖
npm run typecheck    # 类型检查（tsc --noEmit）
npm test             # 运行测试（vitest，39 个）
npm run build        # 类型检查 + 生产构建到 dist/
npm run dev          # 本地开发服务器（http://localhost:5173）
npm run preview      # 预览构建产物（http://localhost:4173）
```

## 测试要求

新增/修改谜题、标准化、进度逻辑时，**必须**同步更新 `tests/`：
- 答案的新等价写法（大小写/分隔符/全角）。
- 搜索命中的新关键词与同义词。
- 节点解锁的新依赖。

## 已记录的合理假设

见 `docs/game_design.md` §八。关键：切片年份 2026；女友名"沈苒"；科学家"林叙之"；口令 `0427ywyxxsc`（可改，集中在 clues.ts）；占位图用 SVG。
